# Vision Audio Graphs — ESP32-S3 Firmware (ESP-IDF)
#
# Website (repo root) is unchanged. All embedded code lives here.
#
cmake_minimum_required(VERSION 3.16)

include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(vision_audio_graphs)
